import { styled } from "@/config/stitches.config";

export const CardCertification = styled("div", {
  background: "$backgroundGray",

  padding: 10,
  borderRadius: 5,
  display: "flex",
  flexDirection: "column",
  gap: 20,
});
